SPECIFICATION Spec

CONSTANTS
    Promises = {stdio, rpath, wpath, cpath, inet}
    Paths = {root, bin, tmp, home}
    Permissions = {read_file, write_file, execute, read_dir, remove}

INVARIANTS
    TypeInvariant
    SecurityInvariant
    PledgeNeverExpands
    ExecNeverExpands
    UnveilPermissionsValid
    ChildCannotExpand
    ChildPolicyValid
    SuccessfulInstallationConsistent
